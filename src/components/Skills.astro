---
const skillGroups = [
  {
    category: "3D Modeling",
    skills: [
      { name: "Revit", level: 88 },
      { name: "3ds Max", level: 90 },
      { name: "Rhino 3D", level: 78 },
      { name: "Rhino Inside", level: 70 },
    ],
  },
  {
    category: "Rendering",
    skills: [
      { name: "D5 Render", level: 95 },
      { name: "Corona Renderer", level: 82 },
    ],
  },
  {
    category: "Design & Post-Production",
    skills: [
      { name: "CorelDRAW", level: 88 },
      { name: "Adobe Photoshop", level: 90 },
      { name: "After Effects", level: 75 },
      { name: "Adobe Premiere", level: 72 },
      { name: "Adobe Illustrator", level: 78 },
    ],
  },
  {
    category: "Computational & AI",
    skills: [
      { name: "Parametric Design", level: 70 },
      { name: "AI Visualization", level: 82 },
      { name: "ComfyUI", level: 72 },
      { name: "Computational Design", level: 68 },
    ],
  },
];
---

<section id="skills">
  <div class="container">
    <p class="section-label">Expertise</p>
    <h2 class="section-title">Skills & Tools</h2>
    <div class="skills-grid">
      {skillGroups.map(group => (
        <div class="skill-group">
          <h3 class="group-title">{group.category}</h3>
          <ul class="skill-list">
            {group.skills.map(skill => (
              <li class="skill-item">
                <div class="skill-header">
                  <span class="skill-name">{skill.name}</span>
                  <span class="skill-pct">{skill.level}%</span>
                </div>
                <div class="skill-bar">
                  <div class="skill-fill" style={`width: ${skill.level}%`}></div>
                </div>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="software-logos">
      <p class="section-label" style="margin-bottom: 32px;">Also proficient in</p>
      <div class="logo-row">
        {["AutoCAD", "SketchUp", "Lumion", "Chaos Vantage", "Canva", "Unreal Engine", "Ghanaian Language", "English Language"].map(tool => (
          <div class="tool-chip">{tool}</div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  #skills { background: var(--off-white); }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 60px 80px;
    margin-bottom: 80px;
  }

  .group-title {
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--black);
    margin-bottom: 28px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--light-gray);
  }

  .skill-list { list-style: none; display: flex; flex-direction: column; gap: 18px; }

  .skill-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 6px;
  }

  .skill-name { font-size: 14px; color: var(--dark-gray); }
  .skill-pct { font-size: 12px; color: var(--mid-gray); }

  .skill-bar {
    height: 2px;
    background: var(--light-gray);
    position: relative;
    overflow: hidden;
  }

  .skill-fill {
    height: 100%;
    background: var(--black);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 1s ease;
  }

  .skill-fill.animated { transform: scaleX(1); }

  .logo-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .tool-chip {
    padding: 8px 18px;
    border: 1px solid var(--light-gray);
    font-size: 12px;
    color: var(--dark-gray);
    background: var(--white);
    letter-spacing: 0.03em;
  }

  @media (max-width: 768px) {
    .skills-grid { grid-template-columns: 1fr; gap: 48px; }
  }
</style>

<script>
  const fills = document.querySelectorAll('.skill-fill');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animated');
      }
    });
  }, { threshold: 0.1 });

  fills.forEach(fill => observer.observe(fill));
</script>
